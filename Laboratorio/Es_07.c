/*
Scrivere due funzioni ricorsive in C:
-   La prima funzione prende in ingresso un array a di interi positivi e un numero n e restituisce un intero
    pari al numero di coppie di numeri specchiati (primo e ultimo, secondo e penultimo) la cui somma è pari a n.
    Si consideri che la somma degli elementi di un array di un elemento solo `e il doppio dell’elemento stesso,
    ovvero l’elemento viene considerato due volte.
-   La seconda funzione prende in ingresso due numeri interi n e m e restituisce il resto della divisione
    intera n/m. Non `e possibile calcolare divisioni (/) o resti (%) nel codice. Si considerino anche i casi in
    cui n o m possano essere 0 o m sia maggiore di n.
*/

